import React, { memo } from 'react';

interface MessageEditPageProps {
  className?: string;
}

const MainPage = memo(({ className }: MessageEditPageProps) => (
  <main>MainPage</main>
));

export default MainPage;
