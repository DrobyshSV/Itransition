import React, { memo } from 'react';

interface UserEditPageProps {
  className?: string;
}

const UserEditPage = memo(({ className }: UserEditPageProps) => (
  <main>UserEditPage</main>
));

export default UserEditPage;
